apiVersion: v1
kind: Service
metadata:
  name: ping
  namespace: grpc-lab
spec:
  selector:
    app: ping
  ports:
    - port: 50505
      protocol: TCP
      targetPort: 50505
  sessionAffinity: None
  type: NodePort